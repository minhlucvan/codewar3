import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fourmilions.danhthiep',
  appName: 'Danh Thiepe',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
